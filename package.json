{
  "name": "networking-platform",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"pnpm --filter client dev\" \"pnpm --filter server dev\"",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "test:server": "pnpm --filter server test",
    "server:watch": "pnpm --filter server test:watch",
    "server:cov": "pnpm --filter server test:cov",
    "server:e2e": "pnpm --filter server test:e2e"
  },
  "author": "thaliszambarda",
  "license": "ISC",
  "packageManager": "pnpm@10.20.0",
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}